<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ldcoding.cryptocurrencyapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="com.ldcoding.cryptocurrencyapp.CoinApplication"
        android:allowBackup="true"
        android:icon="@drawable/cryptomonnaie"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cryptomonnaie"
        android:supportsRtl="true"
        android:theme="@style/Theme.CryptocurrencyApp">
        <activity
            android:name="com.ldcoding.cryptocurrencyapp.presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CryptocurrencyApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>